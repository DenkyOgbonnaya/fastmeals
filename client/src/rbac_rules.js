const rules = {
    0: [
        "profile:visit"
    ],
    1: [
        "admin-board:visit",
        "order:edit",
        "meal:update",
        "meal:delete",
        "profile:visit"
    ]
}
export default rules;